---
// /styleguide è stato unificato dentro /sitemap-team (team hub interno).
// Manteniamo l'URL come alias 301 -> /sitemap-team#tokens per non rompere bookmark.
return Astro.redirect('/sitemap-team#tokens', 301);
---
