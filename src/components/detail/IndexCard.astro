---
import SpokeBadge from './SpokeBadge.astro';

interface Props {
  href: string;
  title: string;
  deck?: string;
  kicker?: string;
  image?: string;
  portrait?: boolean;
  spoke?: string;
}
const { href, title, deck, kicker, image, portrait = false, spoke } = Astro.props;
const cover = image || '/og/placeholder.svg';
---
<a href={href} class:list={['index-card', portrait && 'is-portrait']}>
  <div class="index-card-img">
    <picture>
      {/^.*\.(png|jpe?g)$/i.test(cover) && (
        <source srcset={cover.replace(/\.(png|jpe?g)$/i, '-card.webp')} type="image/webp" />
      )}
      <img src={cover} alt={title} loading="lazy" decoding="async" />
    </picture>
  </div>
  <div class="index-card-body">
    <div class="index-card-meta-row">
      {kicker && <span class="index-card-kicker">{kicker}</span>}
      {spoke && <SpokeBadge spoke={spoke} />}
    </div>
    <h3 class="index-card-title">{title}</h3>
    {deck && <p class="index-card-deck">{deck}</p>}
  </div>
</a>

<style>
  .index-card-meta-row {
    display: flex;
    align-items: center;
    gap: 8px;
    flex-wrap: wrap;
  }
</style>
